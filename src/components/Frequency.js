import React from 'react';
// import './Frequency.css'; 

const Frequency = () => {
  // Image URL relative to the public folder
  const imageUrl = '/img1.png';

  return (
    <div>
      <h1>Frequency</h1>
      <div>
        <img src={imageUrl} alt="Frequency Visual" />
      </div>
    </div>
  );
};

// export default Frequency;
